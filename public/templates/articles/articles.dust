{>"layouts/master_front" /}
{<title}
    Absainte Products
{/title}

{<body}
  <div>
    {?articles}
      {#articles}
      <div>
        <h2>{.title}</h2>
        <p>{.summary}</p>
        <a href="{langPath}/news/{.permalink}">{@pre type="content" key="readNext"/}</a>
      </div>
      {/articles}
    {:else}
        Nothing found :(
    {/articles}
  </div>
{/body}
